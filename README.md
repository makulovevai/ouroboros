# Ouroboros (Уроборос)

Самомодифицирующийся, самоэволюционирующий агент, который:
- запускается в **Google Colab** (одна неизменяемая boot-ячейка),
- общается с владельцем через **Telegram**,
- хранит код и промпты в **GitHub**,
- хранит логи/память в **Google Drive**.

## TL;DR (первый запуск)

1) В Colab добавь Secrets (в меню 🔒):
- `OPENROUTER_API_KEY`
- `TELEGRAM_BOT_TOKEN`
- `TOTAL_BUDGET` (например `100`)
- `GITHUB_TOKEN`
- (опционально) `OPENAI_API_KEY` (для web_search)
- (опционально) `ANTHROPIC_API_KEY` (для `claude_code_edit` через Claude Code CLI)
- (опционально) `GITHUB_USER` (по умолчанию `razzant`)
- (опционально) `GITHUB_REPO` (по умолчанию `ouroboros`)
- (опционально) `OUROBOROS_MODEL` (по умолчанию `openai/gpt-5.2`)

2) Запусти единственную boot-ячейку в Colab.
3) Напиши своему Telegram-боту. Первый написавший становится владельцем.

## Ветки GitHub

- `main` — ветка владельца для правок в Cursor/локально (Уроборос её не использует).
- `ouroboros` — рабочая ветка Уробороса (самоизменения, коммиты).
- `ouroboros-stable` — последняя стабильная версия (для отката).

Супервизор (boot-ячейка) запускает `ouroboros`, а если он падает — откатывается на `ouroboros-stable`.

## Google Drive структура

Папка: `MyDrive/Ouroboros/`

- `state/state.json` — состояние, владелец, бюджет, approvals
- `logs/` — raw JSONL логи (chat, events, tools, supervisor)
- `memory/` — долговременная память
- `index/` — summaries.json и другие индексы
- `NOTES.md` — заметки «что работает / что не работает»

## Команды (Telegram)

Эти команды обрабатываются супервизором (не LLM) и неизменны:
- `/panic` — мгновенно остановить всё (супервизор завершит работу)
- `/restart` — мягко остановить активные задачи, очистить текущий runtime-контекст и перезапустить текущую версию
- `/status` — статус задач/версии/бюджета

## Самоизменение

Канонический способ менять себя:
- использовать tool `repo_write_commit(...)` (запись файла + commit + push в `ouroboros`)
- затем tool `request_restart(...)` (чтобы супервизор перезапустил runtime и подхватил изменения)

Для более качественных многофайловых правок доступен дополнительный путь:
- `claude_code_edit(...)` — делегирует редактирование Claude Code CLI (headless), правит файлы in-place
- `repo_commit_push(...)` — коммитит и пушит уже внесённые изменения (без перезаписи файла целиком)
- `request_restart(...)` — применяет обновлённый код после push

Продвижение в `ouroboros-stable` требует явного подтверждения владельца (approval в чате).

## Telegram: typing indicator

Во время обработки сообщения воркер показывает индикатор `typing…` через Telegram Bot API `sendChatAction`.

Важно: “в момент получения” сообщения (polling/webhook) это делает супервизор; воркер стартует чуть позже,
поэтому typing появляется **как только задача взята в работу**.

Настройки через env:
- `OUROBOROS_TG_TYPING=0/1` (по умолчанию `1`)
- `OUROBOROS_TG_TYPING_INTERVAL` (сек, по умолчанию `4`)

## Telegram: Markdown / formatting

Telegram по умолчанию рендерит разметку только если `sendMessage` вызывается с `parse_mode`.

Так как супервизор (boot-ячейка) сейчас отправляет сообщения как plain text, воркер делает **best-effort** форматирование:
- преобразует небольшой безопасный поднабор Markdown в Telegram HTML (`parse_mode=HTML`),
- и отправляет сообщение **напрямую** через Bot API.

Настройка через env:
- `OUROBOROS_TG_MARKDOWN=0/1` (по умолчанию `1`)

Поддерживаемое (best-effort):
- `**bold**`
- `` `inline code` ``
- fenced code blocks ```...```

## Контекст и устойчивость

- Воркеры подмешивают в контекст хвосты `logs/chat.jsonl`, `logs/tools.jsonl`, `logs/events.jsonl`, `logs/supervisor.jsonl`, `logs/narration.jsonl`, чтобы лучше помнить фактические результаты прошлых шагов.
- При direct-send форматированного ответа в Telegram полный текст всё равно логируется в `chat.jsonl` для следующих задач.
- Long polling Telegram и отправка сообщений обрабатываются с ретраями; кратковременные сетевые таймауты не должны валить супервизор.

## Модели

Пока используется одна модель через OpenRouter: `openai/gpt-5.2` (по умолчанию).
Дальше можно развести на 3 модели (fast / chat / big).
